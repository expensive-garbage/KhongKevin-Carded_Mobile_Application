import 'package:firebase_core/firebase_core.dart';
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:carded/main.dart';
import 'package:carded/login_screen.dart';

void main() {

  // // TODO figure out
  // testWidgets('Pressing the Sign up with email button navigates to a Guest Sign In age', (WidgetTester tester) async {
  //   WidgetsFlutterBinding.ensureInitialized();
  //   await Firebase.initializeApp();
  //
  //   await tester.pumpWidget(const MyApp());
  //
  //   await tester.tap(find.text("Sign Up Page"));
  //   await tester.pumpAndSettle();
  //
  //   await tester.tap(find.text("Sign up with email"));
  //   await tester.pumpAndSettle();
  //
  //   expect(find.text("Sign In"), findsOneWidget);
  // });

}
